[project]
name = "astro-tooling"
version = "0.1.0"
description = "Astro Wars asset generation tooling (SFX, BGM, backgrounds, QA)"
requires-python = ">=3.12"
dependencies = [
  "numpy>=1.26",
  "scipy>=1.13",
  "pillow>=10.0",
]

[tool.ruff]
line-length = 100
